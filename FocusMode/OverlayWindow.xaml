<Window x:Class="FocusMode.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Normal">

    <Grid x:Name="MainGrid">
        <!--
        오버레이는 Path로 구현
        전체 화면을 덮는 검정색 사각형에서 타겟 창 영역만 구멍을 뚫음
        -->
        <Path x:Name="OverlayPath" Fill="Black" Opacity="0.7">
            <Path.Data>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <!-- 전체 화면 -->
                        <RectangleGeometry x:Name="FullScreenRect"/>
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <!-- 구멍 (타겟 창 영역) -->
                        <RectangleGeometry x:Name="HoleRect" RadiusX="8" RadiusY="8"/>
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </Path.Data>
        </Path>
    </Grid>
</Window>
